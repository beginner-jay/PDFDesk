# PDF 정리 프로그램 설계서

- 문서 상태: 구현 전 확정안
- 작성일: 2026-08-15
- 대상 환경: Windows 10/11, macOS
- 대상 사용자: 일반 사무직, 공무원, PC 사용이 익숙하지 않은 사용자

## 1. 제품 목표

별도 교육 없이 PDF를 합치거나 나눌 수 있는 오프라인 데스크톱 프로그램을 만든다.

핵심 목표는 기능 수가 아니라 다음 세 가지다.

1. 처음 사용하는 사람도 바로 이해할 수 있어야 한다.
2. 원본 PDF를 절대로 변경하거나 덮어쓰지 않아야 한다.
3. 많은 페이지를 처리할 때도 프로그램이 멈춘 것처럼 보이지 않아야 한다.

프로그램에서는 개발자 용어인 `병합`, `분할`보다 `합치기`, `나누기`를 우선 사용한다.

## 2. 제품 원칙

- 모든 PDF 처리는 사용자 PC 안에서만 수행한다.
- 서버 전송, 클라우드 저장, 사용자 계정은 사용하지 않는다.
- 기본 화면에는 꼭 필요한 설정만 표시한다.
- 드래그앤드롭을 지원하되 모든 기능을 일반 버튼으로도 사용할 수 있게 한다.
- 아이콘만 있는 버튼보다 `위`, `아래`, `삭제`처럼 의미가 보이는 글자 버튼을 사용한다.
- 작업 결과는 항상 새 파일로 생성한다.
- 오류 메시지는 오류 코드 대신 사용자가 해야 할 행동을 한국어로 안내한다.

## 3. V1 기능 범위

### 3.1 PDF 합치기

- 여러 PDF 동시 선택
- 탐색기 또는 Finder에서 파일 드래그앤드롭
- 파일명과 페이지 수 표시
- 합칠 파일 수와 전체 페이지 수 표시
- 마우스로 파일 행을 끌어서 순서 변경
- `위`, `아래` 버튼으로도 순서 변경
- 개별 파일 삭제
- 저장 위치와 파일명 변경
- 백그라운드 작업, 진행 상태, 취소
- 완료 후 결과 파일 또는 결과 폴더 열기

### 3.2 PDF 나누기

- PDF 한 개 선택
- 파일명, 전체 페이지 수, 파일 크기 표시
- 일정한 페이지 수로 나누기
- 여러 페이지 범위를 직접 지정하여 나누기
- 예상 결과 파일 수와 파일명 표시
- 저장 위치 변경
- 백그라운드 작업, 진행 상태, 취소
- 완료 후 결과 폴더 열기

한 페이지씩 나누기는 별도 기능을 만들지 않는다. `1쪽씩 나누기`로 설정하면 같은 결과를 얻을 수 있다.

## 4. 전체 화면 구조

별도의 시작 화면을 두지 않고 한 창에서 두 기능을 탭으로 전환한다.

```text
┌──────────────────────────────────────────┐
│ PDF 정리                     [사용 방법] │
├──────────────────────────────────────────┤
│ [PDF 합치기]  [PDF 나누기]               │
├──────────────────────────────────────────┤
│                                          │
│              현재 작업 화면              │
│                                          │
└──────────────────────────────────────────┘
```

`사용 방법`에는 다음 세 단계와 원본 보호 안내만 간단히 표시한다.

1. PDF를 선택한다.
2. 순서 또는 나누기 범위를 확인한다.
3. 파란색 실행 버튼을 누른다.

### 4.1 시각 스타일

- 전체 화면은 짙은 회색 배경의 고정 다크 테마를 사용한다.
- 상단에는 파란색 `PDF` 로고, `PDF 정리` 이름, `사용 방법` 버튼을 배치한다.
- 기능 탭은 선택된 항목을 파란 글자와 하단 선으로 표시한다.
- 파일 선택, 원본 정보, 나누기 방식, 예상 결과와 저장 설정은 둥근 카드로 구분한다.
- 기본 버튼은 어두운 회색, 주 실행 버튼은 파란색으로 구분한다.
- 합치기 목록의 각 행에는 드래그 손잡이, 순번, 파일명, 페이지 수, `위`, `아래`, `삭제` 버튼을 표시한다.
- 작은 화면에서는 페이지 내부만 세로로 스크롤하며 상단 브랜드와 기능 탭은 유지한다.

## 5. PDF 합치기 화면

```text
PDF 합치기
파일 행을 마우스로 끌거나 위·아래 버튼을 눌러 순서를 바꿀 수 있습니다.

┌──────────────────────────────────────────┐
│ PDF 파일을 여기에 끌어 놓으세요         │
│                          [PDF 파일 선택] │
└──────────────────────────────────────────┘

⋮⋮  1  공문.pdf              3쪽  [위] [아래] [삭제]
⋮⋮  2  붙임1_사업계획서.pdf 12쪽  [위] [아래] [삭제]
⋮⋮  3  붙임2_산출내역.pdf     7쪽  [위] [아래] [삭제]

총 3개 파일 · 22쪽

저장 위치  문서/PDF 작업                       [변경]
파일 이름  합친파일_20260815.pdf

원본 파일은 변경되지 않습니다.       [3개 파일 합치기]
```

### 5.1 순서 변경

- 파일 행 전체를 마우스로 끌어 원하는 위치에 놓을 수 있다.
- 끌고 있는 행과 놓일 위치를 시각적으로 표시한다.
- 마우스 사용이 어려운 사용자를 위해 `위`, `아래` 버튼을 함께 유지한다.
- 화면의 번호와 실제 합치기 순서는 항상 일치해야 한다.

### 5.2 실행 조건

- 정상 PDF가 두 개 이상 있을 때만 합치기 버튼을 활성화한다.
- 파일을 읽을 수 없으면 해당 파일명과 해결 방법을 표시한다.
- 실행 버튼에는 현재 작업량을 포함해 `3개 파일 합치기`처럼 표시한다.

## 6. PDF 나누기 화면

```text
PDF 나누기

┌──────────────────────────────────────────────┐
│ 나눌 PDF 파일을 여기에 끌어 놓으세요          │
│ 한 개의 PDF 파일을 선택할 수 있습니다.         │ [PDF 파일 선택]
└──────────────────────────────────────────────┘

회의자료.pdf · 327쪽 · 18.4 MB              [삭제]

● 일정한 쪽 수로 나누기
○ 범위를 직접 지정하기

나누기 설정                         예상 결과
[ 50 ]쪽씩 나누기                   7개 파일
                                    회의자료_001-050.pdf
                                    회의자료_051-100.pdf
                                    ...

저장 위치  문서/회의자료_분할                [변경]

원본 파일은 변경되지 않습니다.       [7개 파일로 나누기]
```

- 합치기와 나누기 화면 모두 점선 선택 영역 전체에서 파일 드롭을 받는다.
- 나누기 화면에는 PDF 한 개만 드롭할 수 있으며, 여러 파일이면 한 개만 선택하라는 안내를 표시한다.
- 선택한 파일은 점선 영역 아래의 파일 정보 행에 표시한다.

### 6.1 일정한 페이지 수로 나누기

- 기본값은 50쪽이다.
- 사용자는 숫자 하나만 변경한다.
- 입력 가능 범위는 1부터 원본의 전체 페이지 수까지다.
- 0, 음수, 숫자가 아닌 값은 실행할 수 없으며 입력칸 가까이에 이유를 표시한다.
- 마지막 파일은 남은 페이지까지만 포함한다.

예를 들어 327쪽 문서를 50쪽씩 나누면 7개 파일이 생성된다.

```text
회의자료_001-050.pdf
회의자료_051-100.pdf
회의자료_101-150.pdf
회의자료_151-200.pdf
회의자료_201-250.pdf
회의자료_251-300.pdf
회의자료_301-327.pdf
```

### 6.2 범위를 직접 지정하여 나누기

처음에는 `범위 1` 한 줄만 표시한다. 초기값은 `1~20`이며 원본이 20쪽보다 짧으면 마지막 페이지를 사용한다.

```text
범위 1  [ 1 ]쪽부터 [ 20 ]쪽까지  [삭제]

[범위 추가]
```

`범위 추가`를 누르면 다음 범위가 아래에 생성된다.

```text
범위 1  [ 1 ]쪽부터 [ 20 ]쪽까지  [삭제]
범위 2  [21 ]쪽부터 [ 40 ]쪽까지  [삭제]
범위 3  [41 ]쪽부터 [ 60 ]쪽까지  [삭제]

[범위 추가]
```

범위 처리 규칙은 다음과 같다.

- 각 범위는 별도의 PDF 파일이 된다.
- 범위는 필요한 만큼 추가하거나 삭제할 수 있다.
- 최소 한 개의 범위는 항상 남겨 둔다.
- 시작 페이지는 끝 페이지보다 클 수 없다.
- 모든 값은 1부터 원본의 전체 페이지 수 안에 있어야 한다.
- 서로 겹치는 범위는 허용하지 않는다.
- 입력한 범위의 순서와 결과 파일 표시 순서는 같아야 한다.
- 범위 사이에 빠진 페이지는 허용하되 결과에 포함되지 않는다는 사실을 실행 전에 분명히 알린다.

페이지 누락 안내는 `287쪽은 저장되지 않습니다`처럼 특정 페이지 번호로 오해할 수 있는 표현을 사용하지 않는다.

```text
전체 327쪽 중 40쪽 지정 · 나머지 287쪽은 결과에 포함되지 않습니다.
```

모든 페이지가 포함되면 다음과 같이 표시한다.

```text
전체 327쪽이 결과에 포함됩니다.
```

오류가 있으면 결과 파일 생성 버튼을 비활성화하고 다음처럼 원인을 표시한다.

```text
겹치는 범위가 있습니다.
1~327쪽 안에서 시작과 끝을 확인하세요.
```

## 7. 파일명과 저장 위치

### 7.1 합치기

- 기본 저장 위치: 첫 번째 원본 PDF가 있는 폴더
- 기본 파일명: `합친파일_YYYYMMDD.pdf`
- 같은 이름이 이미 있으면 덮어쓰지 않고 `(1)`, `(2)`를 붙인다.

```text
합친파일_20260815.pdf
합친파일_20260815 (1).pdf
```

### 7.2 나누기

- 기본 저장 위치: 원본 옆의 `<원본명>_분할` 폴더
- 결과 파일명: `<원본명>_<시작쪽>-<끝쪽>.pdf`
- 페이지 번호 자릿수는 전체 페이지 수를 기준으로 맞추되 최소 세 자리로 표시한다.

```text
회의자료_분할/
├── 회의자료_001-050.pdf
├── 회의자료_051-100.pdf
└── 회의자료_101-123.pdf
```

## 8. 작업 진행과 완료 화면

PDF 작업은 GUI와 분리된 백그라운드 작업에서 실행한다.

```text
PDF를 합치고 있습니다.

████████████░░░░  68%

[취소]
```

- 작업 중에도 창이 응답해야 한다.
- 진행률 또는 현재 처리 중인 페이지 수를 표시한다.
- 취소하면 아직 확정하지 않은 임시 결과물을 삭제한다.
- 완료하면 결과 이름과 저장 성공 사실을 표시한다.
- 합치기는 `파일 열기`, 나누기는 `폴더 열기`를 기본 완료 동작으로 제공한다.

## 9. 원본과 결과 파일 보호

다음 규칙은 편의 기능이 아니라 필수 안전 기능이다.

1. 입력 PDF는 읽기 전용으로 사용한다.
2. 최종 경로에 바로 쓰지 않고 같은 저장 위치의 임시 파일에 먼저 쓴다.
3. 쓰기가 끝나면 결과 PDF를 다시 열어 페이지 수를 확인한다.
4. 검증에 성공한 경우에만 최종 파일명으로 변경한다.
5. 실패하거나 취소하면 임시 파일을 삭제한다.
6. 기존 파일은 사용자의 별도 확인 없이 덮어쓰지 않는다.

## 10. 특수 PDF 처리

### 10.1 암호 PDF

V1에서는 암호 입력과 복호화를 지원하지 않는다. 암호 PDF를 선택하면 프로그램이 종료되지 않고 다음 행동을 안내한다.

```text
암호가 설정된 PDF입니다.
암호가 없는 사본을 준비한 후 다시 선택해 주세요.
```

### 10.2 전자서명 PDF

합치거나 나눈 결과에서는 기존 전자서명이 유지되거나 유효하다고 보장할 수 없다. 작업 화면에 다음 내용을 계속 표시한다.

```text
전자서명은 결과 파일에서 유효하지 않을 수 있습니다.
원본 전자서명 문서는 별도로 보관해 주세요.
```

### 10.3 서식과 특수 요소

V1의 주 대상은 일반 문서와 스캔 PDF다. 대화형 서식, 첨부 파일, 멀티미디어, 복잡한 주석과 책갈피는 완전한 보존을 보장하지 않는다.

## 11. 오류 처리

| 상황 | 사용자 안내 및 처리 |
|---|---|
| PDF가 아닌 파일 | PDF 파일만 선택하도록 안내 |
| 손상되었거나 열 수 없는 PDF | 문제가 있는 파일명을 표시하고 작업 제외 또는 중단 |
| 암호 PDF | 암호 없는 사본을 사용하도록 안내 |
| 저장 권한 없음 | 다른 저장 위치를 선택하도록 안내 |
| 같은 결과 파일명 존재 | 자동으로 중복되지 않는 이름 생성 |
| 디스크 공간 부족 | 작업 중단, 임시 파일 삭제 |
| 작업 취소 | 임시 파일 삭제, 원본 유지 |
| 입력 범위 오류 | 입력칸 근처에 이유 표시, 실행 버튼 비활성화 |
| 예상 결과와 페이지 수 불일치 | 결과 확정 금지, 임시 파일 삭제 |

한글, 공백, 괄호, 긴 파일명이 포함된 경로와 OneDrive 또는 네트워크 드라이브는 별도로 시험한다.

## 12. 접근성과 초보 사용자 지원

- 시스템 기본 글꼴과 운영체제 확대 배율을 따른다.
- 중요한 기능을 색상이나 아이콘만으로 구분하지 않는다.
- 모든 입력칸에 보이는 설명을 제공한다.
- 키보드만으로 탭 이동과 버튼 실행이 가능해야 한다.
- 비활성화된 실행 버튼 주변에는 활성화 조건을 알 수 있는 설명을 제공한다.
- 입력 오류는 작업 실행 후가 아니라 가능한 한 입력 즉시 표시한다.
- 드래그앤드롭과 동일한 기능을 버튼으로도 제공한다.

## 13. 기술 구성

| 영역 | 선택 | 역할 |
|---|---|---|
| 언어 | Python | PDF 처리와 데스크톱 앱 개발 |
| GUI | PySide6 / Qt Widgets | Windows와 macOS 공통 화면, 드래그앤드롭, 백그라운드 작업 |
| PDF 엔진 | pypdf | PDF 읽기, 합치기, 페이지 복사와 나누기 |
| 패키징 | PyInstaller 또는 PySide6 공식 배포 도구 | 실행 파일과 앱 번들 생성 |

Windows용 실행 파일은 Windows에서, macOS용 앱은 macOS에서 각각 빌드한다. 외부 배포 단계에서는 Windows 코드 서명과 macOS 서명 및 공증을 검토한다.

## 14. 최소 코드 구조

```text
PDFDesk/
├── main.py
├── pdf_ops.py
├── test_pdf_ops.py
├── pyproject.toml
└── DESIGN.md
```

- `main.py`: 두 작업 화면, 입력 검증, 백그라운드 실행과 완료 안내
- `pdf_ops.py`: PDF 정보 읽기, 합치기, 일정 페이지 수 나누기, 범위 나누기
- `test_pdf_ops.py`: PDF 엔진의 핵심 동작과 실패 시 원본 보호 확인
- `pyproject.toml`: 실행 및 개발 의존성

기능이 실제로 복잡해질 때까지 서비스, 모델, 유틸리티 폴더나 인터페이스 계층을 추가하지 않는다.

## 15. 구현 순서

1. PDF 정보 읽기, 합치기, 두 가지 나누기 기능 구현
2. 결과 페이지 수와 임시 파일 처리 검증
3. 핵심 PDF 작업을 실행하는 작은 자동 테스트 작성
4. 합치기와 나누기 화면 구현
5. 파일 드래그앤드롭과 마우스 순서 변경 구현
6. 백그라운드 실행, 진행 표시, 취소와 완료 화면 구현
7. 한글 경로와 대용량 PDF 시험
8. Windows와 macOS에서 각각 패키징 및 실행 확인

## 16. 완료 기준

다음 조건을 모두 만족하면 V1 구현 완료로 판단한다.

- 합친 결과의 페이지 순서와 화면의 파일 순서가 일치한다.
- 일정 페이지 수로 나눈 모든 결과의 페이지 범위가 예상과 일치한다.
- 여러 지정 범위가 각각 올바른 파일로 저장된다.
- 범위에서 제외된 페이지 수가 실행 전에 정확히 표시된다.
- 범위 중복과 잘못된 페이지 번호를 실행 전에 차단한다.
- 오류, 취소, 강제 종료 상황에서도 원본 PDF가 변경되지 않는다.
- 불완전한 결과 파일이 정상 결과처럼 남지 않는다.
- 처리 중 창이 응답하며 진행 상태가 표시된다.
- 한글 파일명과 한글 폴더 경로에서 정상 동작한다.
- Windows 10/11과 macOS에서 사용자가 Python을 별도로 설치하지 않고 실행할 수 있다.

## 17. V1 제외 기능

- PDF 전체 미리보기와 모든 페이지 썸네일
- 선택 페이지만 하나의 파일로 추출
- 암호 입력 및 복호화
- 파일 용량을 기준으로 나누기
- PDF 압축과 OCR
- PDF와 이미지 간 변환
- 페이지 회전, 삭제, 재배치 편집
- 워터마크와 텍스트 편집
- 전자서명 생성 또는 보존
- 클라우드 저장과 사용자 계정
- 자동 업데이트
- 고급 설정 화면

이 기능들은 실제 사용자 시험에서 반복적으로 필요하다고 확인된 경우에만 후속 버전에서 검토한다.

## 18. 현재 상태

이 문서는 코딩 시작 전에 확인된 설계를 정리한 것이다. 실제 프로그램 구현은 이 설계서가 최종 확인된 뒤 시작한다.
