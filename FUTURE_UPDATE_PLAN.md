# PDFDesk 추후 업데이트 검토 문서

## 1. 문서 목적

이 문서는 PDFDesk의 현재 구현 상태와 지금까지 논의한 기술 결정을 기록하고, 추후 PDF 미리보기 기능 및 프로그램 용량 최적화를 진행할 때 기준으로 사용한다.

## 2. 현재 제품 목표

- Windows 10/11 및 macOS 지원
- 인터넷 연결 없이 PC 내부에서 PDF 처리
- 일반 사무직, 공무원, PC 사용이 익숙하지 않은 사용자도 쉽게 사용
- 여러 PDF 파일 합치기
- PDF를 일정한 페이지 수 또는 직접 지정한 범위로 나누기
- 원본 PDF는 변경하지 않음

## 3. 현재 주요 기능

### PDF 합치기

- 점선 파일 선택 영역에서 여러 PDF 드래그앤드롭
- 파일 선택 버튼 지원
- 파일 행의 드래그 손잡이로 순서 변경
- `위`, `아래` 버튼으로 순서 변경
- 개별 파일 삭제
- 전체 파일 수와 페이지 수 표시
- 저장 위치와 결과 파일명 지정

### PDF 나누기

- 합치기 화면과 동일한 점선 파일 선택 영역 사용
- PDF 한 개 드래그앤드롭 또는 파일 선택
- 여러 파일을 드롭하면 한 개만 선택하라는 안내 표시
- 선택한 파일명, 페이지 수, 용량 표시 및 삭제
- 일정한 페이지 수로 나누기
- 숫자만 입력 가능한 페이지 수 입력칸
- 직접 지정 범위로 나누기
- 기본 범위 한 개 표시 및 범위 추가
- 예상 결과 파일명과 제외되는 페이지 안내

## 4. 현재 기술 구성

| 구분 | 현재 구성 |
|---|---|
| 언어 | Python |
| GUI | PySide6-Essentials / Qt Widgets |
| PDF 처리 | pypdf |
| 패키징 | PyInstaller |
| 테스트 | unittest |

전체 `PySide6` 대신 `PySide6-Essentials`를 사용하고, PyInstaller 설정에서 사용하지 않는 Qt 모듈·플러그인·외국어 번역을 제외했다.

## 5. macOS 앱 용량 최적화 결과

| 구분 | 최적화 전 | 최적화 후 | 감소율 |
|---|---:|---:|---:|
| `.app` | 약 100.6MB | 약 66MB | 약 34% |
| 배포 ZIP | 약 37.1MB | 약 24MB | 약 35% |

- 다운로드용 ZIP은 이미 30MB 이하이다.
- 설치되는 `.app` 자체를 30MB 이하로 만들려면 현재 Python·Qt 구조의 추가 정리만으로는 한계가 있다.
- PyInstaller의 macOS `onefile + windowed` 방식은 실행할 때마다 압축을 풀어야 하고 배포 제약이 있어 우선 선택하지 않는다.

## 6. PDF 미리보기 기능 도입 시 요구사항

추후 미리보기를 추가할 때 다음 기능을 우선 검토한다.

- 선택한 PDF의 첫 페이지 또는 현재 페이지 표시
- 페이지 썸네일 목록
- 이전·다음 페이지 이동
- 확대·축소 및 화면 맞춤
- 합치기 순서와 미리보기 연동
- 나누기 범위와 미리보기 페이지 연동
- 대용량 PDF에서도 UI가 멈추지 않는 비동기 렌더링
- 암호화되거나 손상된 PDF에 대한 오류 안내
- 미리보기 과정에서 원본 파일을 변경하지 않음

## 7. 미리보기 도입 방식 비교

| 방식 | 장점 | 단점 | 30MB 목표 | 플랫폼 |
|---|---|---|---|---|
| 현재 PySide6 + QtPdf | 기존 코드에 가장 쉽게 추가 | QtPdf 및 관련 Qt 모듈로 앱 용량 증가 | 어려움 | Windows/macOS |
| Tauri + PDF.js + Rust | 작은 번들, 동일한 미리보기 UI, 현재 디자인 재현 용이 | UI와 PDF 처리 부분 재개발 필요 | 가능성 높음 | Windows/macOS |
| SwiftUI + PDFKit | macOS에서 가장 자연스럽고 작은 앱 | Windows 버전을 별도로 개발해야 함 | 가능 | macOS 전용 |

## 8. 권장 방향

Windows와 macOS를 계속 지원하면서 설치 앱 30MB 이하와 PDF 미리보기를 모두 목표로 한다면 **Tauri + PDF.js + Rust** 구성을 우선 추천한다.

```text
Tauri 데스크톱 앱
├─ HTML/CSS: 현재 어두운 카드형 UI 재현
├─ PDF.js: 미리보기, 썸네일, 페이지 이동, 확대/축소
└─ Rust: PDF 합치기, 나누기, 파일 저장 및 검증
```

선정 이유:

- Tauri는 운영체제의 WebView를 사용하므로 브라우저 엔진을 앱에 포함하지 않는다.
- PDF.js를 사용하면 Windows와 macOS에서 동일한 렌더링 경험을 제공할 수 있다.
- 현재 화면 디자인을 HTML/CSS로 비교적 쉽게 재현할 수 있다.
- PDF.js는 Apache 2.0 라이선스이므로 배포 시 라이선스와 고지문을 포함하면 사용할 수 있다.

단, 30MB 이하는 확정값이 아니므로 본 개발 전에 대표 PDF로 프로토타입을 빌드해 확인해야 한다.

## 9. 권장 업데이트 순서

### 1단계: 미리보기 프로토타입

- Tauri 기본 앱 생성
- PDF.js로 로컬 PDF 한 개 렌더링
- 첫 페이지, 썸네일, 페이지 이동, 확대·축소 확인
- macOS와 Windows 빌드 용량 측정
- 한글 파일명과 긴 경로 테스트

### 2단계: PDF 처리 검증

- Rust PDF 라이브러리 후보 선정
- 현재 테스트 PDF로 병합·분할 결과 비교
- 페이지 크기, 회전, 암호화, 손상 파일 처리 확인
- pypdf 결과와 동등한지 검증

### 3단계: 현재 UI 이전

- 상단 헤더와 합치기·나누기 탭 재현
- 파일 드래그앤드롭 구현
- 합치기 순서 변경 구현
- 나누기 방식과 범위 추가 UI 구현
- 저장 위치 및 진행 상태 구현

### 4단계: 회귀 테스트 및 전환 결정

- 현재 자동 테스트와 동일한 기능 시나리오 통과
- 대용량 PDF 처리 성능 측정
- macOS와 Windows 설치 파일 용량 비교
- 프로토타입이 요구사항을 만족할 때만 기존 Python 버전을 교체

## 10. 전환 전 반드시 유지할 동작

- 합치기 결과의 파일 순서가 화면 순서와 일치해야 한다.
- 나누기 페이지 번호는 사용자에게 1부터 시작하는 값으로 표시한다.
- 직접 지정 범위에서 빠진 페이지는 저장하지 않고 명확히 안내한다.
- 중복되거나 잘못된 범위는 실행 전에 차단한다.
- 기존 결과 파일을 사용자 동의 없이 덮어쓰지 않는다.
- 취소 시 불완전한 임시 결과를 정리한다.
- 원본 PDF는 절대 변경하지 않는다.
- 모든 드래그앤드롭 기능은 파일 선택 버튼으로도 수행할 수 있어야 한다.

## 11. 최종 판단 기준

다음 조건을 모두 만족할 때 Tauri 기반 버전으로 전환한다.

- macOS와 Windows에서 핵심 기능이 동일하게 작동
- 대표 PDF 병합·분할 결과가 현재 버전과 동등
- 미리보기에서 페이지 이동과 확대·축소가 안정적으로 작동
- 설치 파일 또는 합의한 배포 파일이 목표 용량을 충족
- 일반 사용자가 별도 런타임 설치 없이 실행 가능
- 자동 테스트와 실제 사용자 시나리오 검증 통과

조건을 만족하지 못하면 현재 PySide6 버전을 유지하고, 앱 용량 증가를 허용한 뒤 QtPdf 기반 미리보기를 추가하는 방안을 대안으로 사용한다.
